Контрольные вопросы

1 — Концепции, лежащие в основе стандартной библиотеки.

Стандартная библиотека — набор классов, констант, глобальных переменных, шаблонов, макросов, функций и процедур, доступных для вызова из любой программы, написанной на этом языке и присутствующих во всех реализациях языка.

2 — Системы контроля версий.

Система управления версиями — по для облегчения работы с изменяющейся информацией. Система управления версиями позволяет хранить несколько версий одного и того же документа, при необходимости возвращаться к более ранним версиям, определять, кто и когда сделал то или иное изменение.

3 — Основные действия взаимодействия с git.

git init - создает в текущем каталоге новый подкаталог с именем .git , содержащий все необходимые файлы репозитория.
git clone [url] — получаем копию
git status - после внесения любых изменений в проект можно посмотреть статус файлов с помощью команды
git add - для того, чтобы начать отслеживать новый файл (добавить файл под версионный контроль), используется команда
git diff - если хочется знать, что конкретно поменялось, а не только какие файлы были изменены — вы можете использовать команду
git commit - простейший способ зафиксировать изменения
git push - для размещения локального репозитория на сервере GitHub, нужно
 выполнить команду
git pull — обратная push

4 — Создание отдельных веток для разработки.

Отдельные ветки создают при разработке. Те существует основная и несколько для тестовых версий (модификаций основной), над которыми трудятся отдельные разработчики, в них проверяют работу новых версий проекта, а затем уже обновляют основную ветку.

5 — Библиотека chrono.

Библиотека реализует следующие концепции: интервалы времени – duration; моменты времени – time_point; таймеры – clock.

Функции:
duration_cast -  Приводит объект duration к указанному типу.
time_point_cast - Приводит объект time_point к указанному типу.
Операторы
!= ; +; < ; <=; ==;  и тд...
Структуры 
common_type, duration_values, high_resolution_clock, steady_clock, system_clock, treat_as_floating_point

